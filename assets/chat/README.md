Drop chat-related bundled assets here.

- `evlumba_bg.webp` (optional): Soft background image rendered behind the
  Evlumba Design conversation. Code path: `conversation_detail_screen.dart` →
  `_PremiumBackdrop`. If missing, a soft champagne gradient is used instead.
  Generate with Gemini, keep file size small (under ~120 KB), webp format.
